<%
=begin%>
 <p>The random number is <%= @number %>  </p> 
<%
=end%>

<% content_for :page_title, "Homepage" %>

<nav class="filters"> 
    <div> 
        <%= link_to "All Restaurants", root_path %>
        <%= link_to "$", root_path(price: 1, cuisine: @cusine, location: @location) %>
        <%= link_to "$$", root_path(price: 2, cuisine: @cusine, location: @location) %>
        <%= link_to "$$$", root_path(price: 3, cuisine: @cusine, location: @location) %>

    </div>
    <div>
        <%= link_to "Chinese", root_path(cuisine: "Chinese", price:@price, location: @location) %>
        <%= link_to "Mexican", root_path(cuisine: "Mexican", price:@price, location: @location) %>
        <%= link_to "Italian", root_path(cuisine: "Italian", price:@price, location: @location) %>
    </div>

    <form action="/"> 
        <input type="text" name="location" placeholder="Search Location ..." value="<%= @location  %>">
        <input type="hidden" name="price" value="<%= @price  %>">
        <input type="hidden" name="cuisine" value="<%= @cuisine  %>"> 
    </form>
</nav>

<% @reviews.each do |review| %>

    <div class="review">
        <%= link_to review.title, review_path(review) %>
    </div>

<% end %>